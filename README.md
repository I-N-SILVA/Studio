# Ian Silva Studio

Standalone home for Ian N. Silva's independent AI consulting practice.

Run `npm install` and `npm run dev` locally. The contact form uses Netlify Forms. Set `NEXT_PUBLIC_SITE_URL` to the deployed origin so canonical links and social cards use the correct domain.
